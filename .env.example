# Application Configuration
NODE_ENV=development # development, production, test
PORT=5000
CLIENT_URL=http://localhost:3000 # Frontend URL for CORS and email links

# MongoDB
MONGODB_URI="your_mongodb_connection_string_here"

# JWT Authentication
JWT_SECRET="your_very_strong_jwt_secret"
JWT_EXPIRES_IN="30d" # Example: 30 days
JWT_COOKIE_EXPIRES_IN_DAYS="30" # Example: 30 days

# OpenAI API
OPENAI_API_KEY="your_openai_api_key_here"
OPENAI_MODEL="gpt-3.5-turbo" # Or gpt-4, etc.

# Email Service (Nodemailer with Gmail example)
# IMPORTANT: For production, consider using OAuth2 or an app-specific password for Gmail,
# or a dedicated email transaction service (e.g., SendGrid, Mailgun).
EMAIL_FROM="your_email@example.com"
EMAIL_PASS="your_email_password_or_app_specific_password" # See important note above

# Razorpay Payment Gateway
RAZORPAY_KEY_ID="your_razorpay_key_id"
RAZORPAY_KEY_SECRET="your_razorpay_key_secret"

# Redis Cache
REDIS_URL="redis://localhost:6379" # Example: redis://username:password@host:port

# Add any other environment specific variables your application might need
# Example: LOG_LEVEL=info
# Example: API_RATE_LIMIT_MAX=100
# Example: API_RATE_LIMIT_WINDOW_MS=900000 (15 mins)
